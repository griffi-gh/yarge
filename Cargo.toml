[workspace]
members = [
  "yarge-core",
  "yarge-frontend-desktop",
  "yarge-frontend-nano",
  "yarge-rom-tests"
]
resolver = "2"

[profile.dev.package."*"]
opt-level = 3

[profile.production]
inherits = "release"
strip = true
codegen-units = 1
lto = true
panic = "abort"
